<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @poll.name %>
</p>

  <strong>Records:</strong>
  <ul>
    <% @poll.records.each do |record| %>
      <%= render record %>
    <% end %>
  </ul>

<%= link_to 'Edit', edit_poll_path(@poll) %> |
<%= link_to 'Back', polls_path %>
