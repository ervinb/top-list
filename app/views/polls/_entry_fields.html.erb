<%= builder.fields_for :entries do |f| %>
  <p>
    <%= f.label :name, "Entry" %>
    <%= f.text_field :name %>
    <%= f.hidden_field :_destroy %>
    <%= f.link_to_remove 'Remove' %>
  </p>
<% end %>

<p> <%= builder.link_to_add "Add an entry", :entries %> </p>
